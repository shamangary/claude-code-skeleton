---
matching:
  paths:
  - Script/run_devcontainer_claude_code.ps1
---

# `Script/`

**Role:** **Windows helper** for running Claude Code in a **dev container** context (PowerShell entrypoint).

| Upstream file | Macro meaning |
|---------------|----------------|
| `run_devcontainer_claude_code.ps1` | Script to launch/use the devcontainer workflow from Windows. |

**Audience:** Contributors on Windows using the repo’s devcontainer setup.

**Upstream:** [`../../claude-code/Script/`](../../claude-code/Script/). Container definition: [`../.devcontainer/MACRO.md`](../.devcontainer/MACRO.md).

**Neighbors:** Root [`MACRO.md`](../MACRO.md).
