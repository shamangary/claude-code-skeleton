---
matching:
  paths:
  - plugins/plugin-dev/skills/hook-development/SKILL.md
---

# Skill: `hook-development`

**Role:** Teaches **event-driven hooks** in Claude Code: when to use `PreToolUse`, schema expectations, bash vs python, migration from older hook styles, and validation tooling.

**Contents:**

| Subfolder | Macro meaning |
|-----------|----------------|
| `SKILL.md` | Entry: triggers (“add a hook…”) + essential patterns. |
| `references/` | Long-form patterns, migrations, advanced scenarios (**one corpus**). |
| `examples/` | Runnable hook examples (`*.sh` snippets). |
| `scripts/` | `validate-hook-schema.sh`, `hook-linter.sh`, `test-hook.sh` helpers for authors. |

**Neighbors:** [Skills catalog](../MACRO.md).
