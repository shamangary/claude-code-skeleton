---
matching:
  paths:
  - plugins/plugin-dev/skills/mcp-integration/SKILL.md
---

# Skill: `mcp-integration`

**Role:** Guides **Model Context Protocol** integration: server flavors, authentication concerns, and how Claude Code discovers tools from MCP config.

**Contents:**

| Subfolder | Macro meaning |
|-----------|----------------|
| `SKILL.md` | Entry triggers + decision tree for MCP adoption. |
| `references/` | Tool usage, auth flows, server archetypes (**cluster**). |
| `examples/` | Example JSON stubs for `stdio`, `sse`, and `http` servers. |

**Neighbors:** [Skills catalog](../MACRO.md).
