---
matching:
  paths:
  - plugins/hookify/matchers/__init__.py
---

# Package: `hookify/matchers`

**Role:** **Matching logic** that classifies tool payloads / prompts so rules can target specific situations (e.g. destructive commands, sensitive paths).

**Audience:** Hookify implementation; advanced rule authors (indirectly).

**Neighbors:** [`../core/MACRO.md`](../core/MACRO.md).
