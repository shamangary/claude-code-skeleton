---
matching:
  paths:
  - plugins/commit-commands/README.md
---

# Plugin: `commit-commands`

**Role:** Shortens **git** workflows via slash commands: commit, push + PR, and **cleanup** helpers.

**Audience:** Daily developers using Claude Code as a git companion.

**Surfaces:** Commands only (no agents/skills in this plugin).

**Neighbors:** [`.claude-plugin/MACRO.md`](.claude-plugin/MACRO.md); [`commands/`](commands/) (`*.macro.md`).

**Stability:** User-facing; renames would break muscle memory—expect additive changes only.
