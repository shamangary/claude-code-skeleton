---
matching:
  paths:
  - plugins/agent-sdk-dev/.claude-plugin/plugin.json
---

# Manifest: `agent-sdk-dev`

**Role:** Declares plugin identity (`plugin.json`) for **Claude Agent SDK development** support.

**Audience:** Claude Code loader / marketplaces.

**Inputs / outputs:** Metadata only; behavior is documented per file in [`../commands/`](../commands/) and [`../agents/`](../agents/) as `*.macro.md`.

**Neighbors:** [Plugin overview](../MACRO.md).

**Stability:** Contract surface for installation and discovery.
